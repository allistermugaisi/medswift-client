import Head from 'next/head';
import Image from 'next/image';
import { Layout, MainSection } from '../components';

const Home = () => {
	return (
		<Layout>
			<MainSection />
		</Layout>
	);
};
export default Home;
